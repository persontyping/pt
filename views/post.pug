extends layout

block content
  article
    h1= post.title

    if post.createdAt
      small.text-muted Published on #{post.createdAt}

    p !{post.body}

    if post.imageUrl
      img(
        src='/img/' + post.imageUrl,
        alt=post.title || 'Post image',
        class="img-fluid",
        style="max-width: 700px; max-height: 300px;"
      )
    else
      img(
        src="/img/call-me.svg",
        alt=post.title || 'Placeholder image',
        class="d-block mx-auto",
        style="max-width: 300px; height: auto;"
      )

    hr

    a(href="/blog") ← Back to blog
